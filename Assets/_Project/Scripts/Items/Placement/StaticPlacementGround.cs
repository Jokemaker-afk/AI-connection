using UnityEngine;

/// <summary>
/// Marks static terrain/floor colliders as valid placement support without blocking overlap checks.
/// Attach to scene ground geometry — not to placed objects.
/// </summary>
public class StaticPlacementGround : MonoBehaviour
{
}
